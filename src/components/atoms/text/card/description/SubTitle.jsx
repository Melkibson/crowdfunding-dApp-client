import React from 'react'

const SubTitle = ({content}) => {
    return (
        <p className="mt-[5px] font-epilogue font-normal prose-sm text-gray-400 text-left leading-[18px] truncate">{content}</p>

    );
}

export default SubTitle
