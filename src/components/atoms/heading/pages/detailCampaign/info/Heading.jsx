import React from 'react'

const Heading = ({content}) => {
    return (
        <h4 className="font-epilogue font-semibold text-[14px] text-white break-all">{content}</h4>
    );
}

export default Heading
